# VibeHub UI Design Spec · V3

VibeHub 是预置工具合集，不是生成器或托管管理后台。本规范替代旧版 AI/Builder 界面说明。

## 首页

首页只展示 `tools.json` 定义的工具卡片。整张卡片可点击，包含图标、名称、简短用途与进入提示。保留统一明暗主题；不展示 AI、新建、扫描、导入、编辑、删除、重命名、手动启停、日志、代码或管理员入口。

Web 版存在有效 Windows 发行文件时才展示客户端下载链接；桌面版不重复展示下载自身的按钮。

## 工具容器

用户入口 `/tool/<id>`。所有工具共享 `ToolHost`，顶部始终提供明确的“返回首页”与当前工具名称，下方 iframe 承载 `/tools/<id>/`。不在每个工具里复制导航代码，也不依据浏览器历史猜测首页。

点击后由平台自动准备运行环境并启动。状态只需要准备中、可用、失败重试，不把底层进程管理暴露为产品功能。返回首页仅隐藏已打开的容器；同一会话再次进入不主动重载 iframe。

工具内部保留自身界面、上传、拖拽、预览、下载等现有能力。公共层不能重写工具的表单提交或资源 URL。

## 双端一致性与验收

浏览器和 WebView2 加载同一套 React 页面及 HTTP API。桌面差异仅位于入口、窗口、浏览器下载配置、数据目录与退出清理。

必须验证首页卡片、统一返回、失败重试、上传及下载、返回后状态保留、窄窗口下返回入口可见性。截图测试环境应具备中文字体，不把测试机缺字误判为源码乱码。

新增工具遵循 `docs/TOOL_SPEC.md`，维护流程遵循 `AGENTS.md`。
